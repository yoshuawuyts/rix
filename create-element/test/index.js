
const test  = require('tape')
const create = require('../')

test('create() should assert input types', function(t) {
  t.end()
  // t.equal(create(function(){}, {}),)
})

test('create() should diff trees')

test('create() should create a RAF loop')

test('create() should only render a tree if state is dirty')
